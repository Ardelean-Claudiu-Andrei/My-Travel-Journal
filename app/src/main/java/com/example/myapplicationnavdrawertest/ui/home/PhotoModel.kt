package com.example.mytraveljournal.ui.home

data class PhotoModel(
    val photoResourceId: Int // This could represent your photo's resource ID
    // Add other properties if needed, like photo title, description, etc.
)
