<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <!--    android:background="@android:color/white"-->


    <!--     Circular background for the profile picture -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/circularBackground"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginTop="104dp"
        android:src="@drawable/circle_background"
        android:contentDescription="Profile Picture Zone"
        android:scaleType="centerCrop"
        android:layout_gravity="center_horizontal" />

    <!-- Profile Picture Image -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profilePicture"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_marginTop="106dp"
        android:src="@drawable/ic_profile"
        android:textColor="?android:textColorPrimary"
        android:contentDescription="Profile Picture"
        android:scaleType="centerCrop"
        android:layout_gravity="center_horizontal" />

    <!-- Button -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="200dp"
        android:padding="16dp">

        <!-- Name TextView and TextBox -->
        <TextView
            android:id="@+id/textViewName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="Name"
            android:textColor="?android:textColorPrimary"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/userNameLogin"
            android:layout_below="@id/textViewName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/name"
            android:textColor="?android:textColorPrimary"
            android:padding="8dp"
            android:layout_marginBottom="16dp"/>

        <TextView
            android:id="@+id/textViewEmail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/userNameLogin"
            android:layout_marginTop="24dp"
            android:text="Email"
            android:textColor="?android:textColorPrimary"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/userEmailLogin"
            android:layout_below="@id/textViewEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/name"
            android:textColor="?android:textColorPrimary"
            android:padding="8dp"
            android:layout_marginBottom="16dp"/>

    </RelativeLayout>

    <Button
        android:id="@+id/buttonLogOut"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="611dp" />

    <!-- Other placeholders or UI elements can be added similarly -->
</FrameLayout>
